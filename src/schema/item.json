{
  "title" : "item",
  "description": "Description de l'item",
    "type": "object",
    "properties": {
      "id": {
        "type": "string"
      },
      "titre": {
        "type": "string"
      },
      "type": {
        "type": "string",
        "enum": ["film", "série"]
      },
      "état": {
        "type": "string",
        "enum": ["A voir", "En cours", "Terminé", "Abandonné"]
      }
    },
    "required": ["id", "titre", "type", "état"]
  }